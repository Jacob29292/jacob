import { useMemo, useState } from 'react';import { useGameStore } from '../store/gameStore';import { levels } from '../data/levels';
export default function CombatScreen(){const {level,winLevel,setScreen}=useGameStore();const lv=useMemo(()=>levels.find(l=>l.id===level)!,[level]);const [hp,setHp]=useState(100);
return <div className='space-y-3'><div>Cœur Mutant HP: {hp}</div><div className='h-36 rounded bg-slate-900 p-2 flex items-center justify-between'><span>Spawn</span><span>Slots {lv.slots}</span><span>Core</span></div><div className='grid grid-cols-2 gap-2'><button className='p-3 bg-green-600 rounded' onClick={()=>winLevel(level,lv.eggReward)}>Simuler Victory</button><button className='p-3 bg-red-600 rounded' onClick={()=>{setHp(0);setScreen('defeat')}}>Simuler Defeat</button></div></div>}
