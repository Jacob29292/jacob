import { motion } from 'framer-motion';import { kaijus } from '../data/kaijus';import { useGameStore } from '../store/gameStore';
export default function(){const {opening,closeOpening}=useGameStore();if(!opening)return null;const k=kaijus.find(x=>x.id===opening)!;return <div className='fixed inset-0 bg-black/80 grid place-items-center z-50'><motion.div initial={{scale:0.4,rotate:-10}} animate={{scale:[0.4,1.1,1],rotate:[-10,10,0]}} transition={{duration:1.5}} className='text-center'><div className='text-8xl mb-3'>{k.emoji}</div><h3 className='text-3xl font-bold' style={{color:k.accent}}>{k.name}</h3><p>{k.rarity.toUpperCase()}</p><button onClick={closeOpening} className='mt-4 px-6 py-3 rounded-xl bg-fuchsia-600'>AWESOME</button></motion.div></div>}
