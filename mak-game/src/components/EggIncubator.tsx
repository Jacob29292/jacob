import { useGameStore } from '../store/gameStore';
export default function EggIncubator(){const {eggs,openEgg,setScreen}=useGameStore();return <div><h2>Incubateur</h2><div className='space-y-2'>{[0,1,2,3].map(i=>{const e=eggs[i]; if(!e)return <div key={i} className='p-3 bg-slate-800 rounded'>Slot vide</div>;const ready=e.readyAt<=Date.now(); return <div key={e.id} className='p-3 bg-slate-800 rounded'>{e.rarity} {ready?<button onClick={()=>openEgg(e.id)}>OUVRIR</button>:<button onClick={()=>openEgg(e.id)}>SKIP</button>}</div>})}</div><button className='mt-3' onClick={()=>setScreen('home')}>Retour</button></div>}
