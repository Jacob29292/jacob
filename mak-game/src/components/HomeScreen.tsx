import { useGameStore } from '../store/gameStore';
export default function(){const {setScreen,eggs}=useGameStore(); const ready=eggs.some(e=>Date.now()>=e.readyAt); return <div className='space-y-4 py-8'><h1 className='text-center text-4xl font-bold'>M.A.K.</h1><div className='text-center text-7xl animate-pulse'>🧬</div>{[['JOUER','levels'],['INCUBATEUR','incubator'],['DECK','deck'],['COLLECTION','collection']].map(([l,s])=><button key={l} onClick={()=>setScreen(s as any)} className='w-full py-4 rounded-2xl bg-fuchsia-600 font-bold'>{l}{l==='INCUBATEUR'&&ready?' • !':''}</button>)}</div>}
