import { useGameStore } from '../store/gameStore';import { parts } from '../data/parts';
const Btn=({t,s}:{t:string;s:any})=><button onClick={()=>useGameStore.getState().setScreen(s)} className='bg-pink-500 rounded-2xl p-3 font-bold shadow-lg active:translate-y-1'>{t}</button>;
export default function HomeScreen(){const {equipped,eggs}=useGameStore();const eq=Object.values(equipped).map(id=>parts.find(p=>p.id===id));const ready=eggs.filter(e=>e.readyAt<Date.now()).length;return <div className='p-4 text-white'><div className='text-center text-3xl font-bold'>Ton Kaiju</div><div className='my-8 text-7xl text-center'>{eq.map(p=><span key={p?.id} style={{color:p?.color}}>{p?.visualKey}</span>)}</div><div className='grid grid-cols-2 gap-3'><Btn t='COMBAT' s='combat'/><button onClick={()=>useGameStore.getState().setScreen('incubator')} className='bg-cyan-500 rounded-2xl p-3 font-bold shadow-lg'>INCUBATEUR {ready>0&&<span className='ml-1 bg-yellow-300 text-black px-2 rounded-full'>{ready}</span>}</button><Btn t='KAIJU BUILDER' s='builder'/><Btn t='COLLECTION' s='collection'/></div></div>}
