import { parts } from '../data/parts';import { useGameStore } from '../store/gameStore';
export default function KaijuBuilder(){const {owned,equipped,equip}=useGameStore();return <div className='p-4 text-white'><div className='text-6xl'>{Object.values(equipped).map(id=>{const p=parts.find(x=>x.id===id);return <span key={id} style={{color:p?.color}}>{p?.visualKey}</span>})}</div>{(['head','torso','arms','legs','aura'] as const).map(c=><div key={c}><h3 className='font-bold mt-3'>{c}</h3><div className='flex gap-2 overflow-auto'>{parts.filter(p=>p.category===c&&owned.includes(p.id)).map(p=><button key={p.id} onClick={()=>equip(p)} style={{borderColor:p.color}} className='border p-2 rounded'>{p.visualKey}</button>)}</div></div>)}</div>}
