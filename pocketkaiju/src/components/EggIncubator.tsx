import { useEffect, useState } from 'react';import { useGameStore } from '../store/gameStore';
export default function EggIncubator(){const {eggs,openEgg,skipEgg}=useGameStore();const [now,setNow]=useState(Date.now());useEffect(()=>{const i=setInterval(()=>setNow(Date.now()),250);return()=>clearInterval(i)},[]);
return <div className='p-4 text-white'><h2 className='text-2xl font-bold'>Incubateur</h2><div className='grid gap-3 mt-3'>{Array.from({length:4}).map((_,i)=>{const e=eggs[i];if(!e)return <div key={i} className='p-4 rounded-xl bg-white/10'>Slot vide</div>;const ready=now>=e.readyAt;return <div key={e.id} className='p-4 rounded-xl bg-white/10'>{e.tier} {ready?'✅ prêt':`${Math.ceil((e.readyAt-now)/1000)}s`} <button onClick={()=>skipEgg(e.id)} className='ml-2 bg-cyan-500 px-2 rounded'>SKIP</button>{ready&&<button onClick={()=>openEgg(e.id)} className='ml-2 bg-yellow-500 px-2 rounded'>OUVRIR</button>}</div>})}</div></div>}
